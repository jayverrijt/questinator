@page
@model LoginModel
@{
    Layout = "_IdentityLayout";
    ViewData["Title"] = "Login - Questinator";
}

<body class="d-flex flex-column min-vh-100">

    <!-- Hero / Spotlight Header -->
    <header class="py-5 text-center hero-header-spotlight">
        <div class="container">
            <h1 class="display-4 fw-bold fade-in">Login bij Questinator</h1>
            <p class="lead fade-in" style="animation-delay:0.2s;">
                Voer je gegevens in om toegang te krijgen tot alle functies van Questinator.
            </p>
        </div>
    </header>

    <!-- Login Form -->
    <section class="container my-5 flex-grow-1">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card p-4 fade-in" style="animation-delay:0.4s;">
                    <div class="card-body">
                        <form method="post">
                            <div asp-validation-summary="All" class="text-danger mb-3"></div>

                            <div class="mb-3">
                                <label asp-for="Input.Email" class="form-label"></label>
                                <input asp-for="Input.Email" class="form-control"/>
                                <span asp-validation-for="Input.Email" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <label asp-for="Input.Password" class="form-label"></label>
                                <input asp-for="Input.Password" class="form-control"/>
                                <span asp-validation-for="Input.Password" class="text-danger"></span>
                            </div>

                            <div class="mb-3 form-check">
                                <input asp-for="Input.RememberMe" class="form-check-input"/>
                                <label asp-for="Input.RememberMe" class="form-check-label"></label>
                            </div>

                            <button type="submit" class="btn btn-primary w-100">Login</button>
                        </form>

                        <p class="mt-3 text-center">
                            Nog geen account? <a href="/Identity/Account/Register">Registreren</a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

</body>
